name: Env Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  env_demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-24.04
    steps:
      - name: Connecting to Env
        env:
          APP_ENV: "prod"
        run: echo "App is connecting to $APP_ENV"
        
