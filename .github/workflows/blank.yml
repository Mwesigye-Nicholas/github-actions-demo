name: Hello World
on: push
jobs:
   build: #job one
      runs-on: ubuntu-latest
      steps:
         - name: Build Step
           run: |
              echo "A demo build"
              echo "Hello, ${{ vars.MY_NAME}}
